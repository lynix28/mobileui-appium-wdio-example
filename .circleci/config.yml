version: 2.1
jobs:
  Testing:
    docker:
      - image: cimg/node:20.2.0
    steps:
      - checkout
      - run:
          name: "Install Java"
          command: |
            apt install default-jre
            java --version
      - run:
          name: "Setup Environment Variable"
          command: |
            echo "SAUCE_USERNAME=$SAUCE_USERNAME" > .env
            echo "SAUCE_ACCESS_KEY=$SAUCE_ACCESS_KEY" >> .env
            echo "BUILD=v1.0.0" >> .env
            echo "BUILDNAME=Testing" >> .env
      - run:
          name: "Install Dependencies"
          command: npm install
      - run:
          name: "Running Test"
          command: npm run cloud -- --suite "negativeTest"
      - run:
          name: "Wrapping Test Result"
          command: "tar -cvf artifact.tar ./allure-report/"
      - store_artifacts:
          path: artifact.tar
          
workflows:
  Test-Workflow:
    jobs:
      - Testing
